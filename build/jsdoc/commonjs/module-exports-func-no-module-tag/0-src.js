/* This file produces no output, creating the most common confusion users report. Should cause dmd to print a warning. */

/**
 * Add two values.
 */
module.exports = function add (a, b) {
  return a + b
}
