/**
 * A module for adding two values.
 * @module add-two-values
 */

define(function () {
  /**
   * Add two values.
   * @alias module:add-two-values
   */
  function add (a, b) {
    return a + b
  }

  return add
})
